#include <stdio.h>

 int mul(int num){
 int mul = 1 ; 
 if(num < 0){ 
  num = -num;
 }  
  return(num%10) * (num / 10);
 } 
 
 
 
 int main(){
  int res = 0;
  int num = 0;
  printf("Enter the number");
  scanf("%d",&num);
  res = mul(num);
  printf("Resalt is:%d\n" , res);
  return 0;
 }
