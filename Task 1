#include <stdio.h>

int fib(int n){
 if(n >= 0 && n < 2){
 return n;
 }
 return fib(n - 1) + fib(n - 2);
}
 int main(){
  int res = 0;
  int n = 0;
  printf("Enter the number");
  scanf("%d",&n);
  res =  fib(n);
  printf("%d",res);
  return 0;
 }
